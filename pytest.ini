[pytest]
testpaths = tests
addopts = -ra --strict-markers --html=reports/pytest-report.html --self-contained-html --junitxml=reports/junit.xml
markers =
    accessibility: Accessibility checks using automated rules or browser semantics.
    api: Direct REST API tests that do not use the browser interface.
    authorization: Access-control and resource-ownership boundary checks.
    contract: HTTP status, content, and response-schema contract checks.
    database: Direct database schema, constraint, persistence, and SQL validation.
    functional: User-visible behavior checks against approved requirements.
    integration: Cross-layer comparisons between service responses and persisted state.
    negative: Invalid-input and denied-workflow checks.
    performance: Performance-support checks and explicitly selected local baselines.
    regression: Stable checks rerun to detect unintended behavior changes.
    smoke: Critical checks that determine whether broader testing can begin.
    ui: Browser-based user-interface tests.
    uat: Browser replays and artifact checks supporting the simulated UAT cycle.
